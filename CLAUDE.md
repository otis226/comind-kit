# Claude Code repository guidance

Read and follow `AGENTS.md` before editing this repository.

For runtime behavior:

- canonical reusable workflows live in `skills/`;
- Claude Code specialist adapters live in `agents/`;
- plugin distribution metadata lives in `.claude-plugin/`.

Do not treat `README.md` as the canonical agent policy.
